# CI failure canary

This temporary pull-request marker keeps the recovery revision docs-only after
the deliberately failing contract fixture is removed. The pull request is
closed without merge after both failure and recovery evidence are captured.
